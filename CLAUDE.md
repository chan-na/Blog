# Blog

Hugo + PaperMod 기반 이중언어(한/영) 블로그. 포스트는 `content/posts/<slug>.md`(한글)와 `content/posts/<slug>.en.md`(영문) 한 쌍으로 존재하며, 두 파일은 섹션 단위로 1:1 대응한다.

## 디렉터리

- `content/posts/` — 발행된 글. `<slug>.md`(한글) + `<slug>.en.md`(영문) 한 쌍.
- `ideas/` — 아직 쓰지 않은 글감. 글감 하나에 파일 하나, 목록은 `ideas/README.md`. Hugo가 읽지 않으므로 빌드에 영향이 없다.
- `_workspace/` — `/post` 스킬의 중간 산출물. gitignore 대상이라 남길 것을 두면 안 된다.

**본문에서 덜어낸 내용은 버리지 말고 `ideas/`로 옮긴다.** 주제가 글의 결과 맞지 않아 빼는 경우, 초안·명령어 출력·왜 뺐는지를 글감 파일에 그대로 남겨 다음 글의 출발점으로 쓴다.

## 포스트 문체 규약

사용자 피드백에서 일반화된 규칙만 여기 남긴다(개별 글의 일회성 수정은 제외).

**규칙을 추가할 때** — 규칙 한 줄 + ✗/✓ 예시를 함께 적는다. 예시는 그 자체로 상황이 읽혀야 한다. "그림 라벨의 '고치는 곳'" 같은 축약형은 그 글을 읽은 사람에게만 통하고, 아무 맥락 없이 이 파일만 읽는 다음 세션에서는 작동하지 않는다. 예시 문장은 통째로, 필요하면 어떤 자리에 쓰인 문장인지(본문·figcaption·그림 라벨·섹션 제목)까지 밝힌다.

### 한국어

- **추상적 비유 서술어 대신 직설적인 동사를 쓴다.**
  - ✗ 앞의 두 글에서 "리졸버"라는 말이 한 번씩 **지나갔다**.
  - ✓ 앞의 두 글에서 "리졸버"라는 말이 한 번씩 **언급되었다**.

- **사람이나 사물에 쓰는 말을 개념에 붙이지 않는다.** "물건"은 실제 사물에만 쓰고, 개념에는 "대상·것"을 쓴다. "직업"은 사람이 갖는 것이므로 개념에는 "역할"을 쓴다.
  - ✗ 둘 다 DNS 얘기인데 가리키는 **물건이** 정반대다. / "DNS 서버"는 **직업이** 두 개다.
  - ✓ 둘 다 DNS 얘기인데 가리키는 **대상이** 정반대다. / "DNS 서버"는 **역할이** 두 개다.

- **비유로 설명을 대신하지 않는다.** 어원이나 비유는 곁들이되, 실제로 무엇을 하는지는 따로 적는다.
  - ✗ 스텁(stub)은 그루터기라는 뜻이다. **잘려나가고 남은 최소한만 한다.**
  - ✓ 스텁(stub)은 그루터기라는 뜻이다. **스텁 리졸버는 캐시를 확인하고, 없으면 재귀 리졸버에 질의를 넘긴 뒤 그 답을 프로그램에 돌려주는 일만 한다.**

- **지시대명사로 뭉뚱그리지 않고 대상의 이름을 쓴다.**
  - ✗ **하나는** 내가 누구에게 묻느냐, **하나는** 세상이 내 도메인을 누구에게 묻느냐다.
  - ✓ **리졸버는** 내가 누구에게 묻느냐, **권한 네임서버는** 세상이 내 도메인을 누구에게 묻느냐다.
  - ✗ 내 컴퓨터 안에도 **작은 게** 하나 더 있다. → ✓ 내 컴퓨터 안에도 **작은 리졸버가** 하나 더 있다.

- **주어를 생략하지 않는다.** figcaption·그림 설명처럼 본문에서 떨어져 단독으로 읽히는 자리에서는 특히 그렇다. 아래 ✗는 두 번째 문장의 주어가 "그루터기"인지 "스텁 리졸버"인지 알 수 없다.
  - ✗ (figcaption) 스텁(stub)은 그루터기라는 뜻이다. 잘려나가고 남은 최소한만 한다.
  - ✓ (figcaption) 스텁(stub)은 그루터기라는 뜻이다. **스텁 리졸버는** …

- **목적어를 생략하지 않는다.** 그림 라벨은 문장에서 떨어져 단독으로 읽히므로, 무엇에 대한 말인지가 라벨 안에 있어야 한다. 아래는 부모 존·자식 존 두 카드가 각각 "어디서 수정하는가"를 적은 라벨이다.
  - ✗ 고치는 곳: 레지스트라 화면
  - ✓ **이 NS 레코드를** 고치는 곳: 레지스트라 화면

### 구성

- **혼동을 지적할 때는 무엇과 무엇이 헷갈리는지 후보를 밝힌다.**
  - ✗ 그런데 정작 **이 두 문장이 뒤섞인다.**
  - ✓ 아래 두 문장을 보면, 각각이 말하는 서버가 **리졸버인지 권한 네임서버인지** 헷갈릴 수 있다.

- **예시를 늘어놓았으면 바로 답을 붙인다.** 독자가 알아서 대응시키게 두지 않는다. 아래는 "DNS 서버를 8.8.8.8로 바꿔봐" / "네임서버를 Cloudflare로 바꿔야 해" 두 예문을 보여준 다음 문단이다.
  - ✗ 둘 다 DNS 얘기인데 가리키는 대상이 정반대다. (어느 문장이 어느 쪽인지는 안 밝힘)
  - ✓ **앞 문장의 `8.8.8.8`은 내가 질문을 던지는 쪽인 리졸버고, 뒤 문장의 네임서버는 세상이 내 도메인을 물어보는 쪽인 권한 네임서버다.** 둘 다 DNS 얘기인데 가리키는 대상이 정반대다.

- **섹션 제목은 비유가 아니라 내용 요약이다.** 아래는 "DNS 질의가 브라우저 → OS 스텁 리졸버 → 재귀 리졸버 → 권한 네임서버를 차례로 거친다"를 설명하는 섹션의 제목이다.
  - ✗ `## 질문이 지나가는 네 층`
  - ✓ `## DNS 질의는 네 층을 거친다`

- **다른 글을 참조할 때는 제목과 링크를 밝힌다.**
  - ✗ 구별하는 방법은 **앞 글에서** 이미 봤다.
  - ✓ 구별하는 방법은 **[「도메인 등록, 그림으로 이해하기」](/posts/how-domains-get-registered/)에서** 이미 봤다.

- **참조한 내용은 이 글만 읽어도 통하게 쓴다.** 앞 글의 사례를 끌어올 때는 증상·명령·결과를 다시 적는다. 독자가 앞 글을 읽었다고 전제하지 않는다.
  - ✗ 앞 글에서 제일 오래 붙잡았던 **"`dig`는 되는데 `curl`은 안 된다"**가 정확히 이 그림이다.
  - ✓ [「도메인 사서 웹페이지 붙이기, 실제로 해봤다」](/posts/buying-a-domain-in-practice/)에서 제일 오래 붙잡았던 문제가 정확히 이 그림이다. **같은 컴퓨터에서 같은 시각에 `dig byeorim.com A`는 IP를 제대로 뱉는데 `curl https://byeorim.com`은 `Could not resolve host`로 실패했다.**

### 용어

- **기술 용어는 영문을 병기한다.** 용어를 정의하는 자리에 한 번만 붙이고, 이후 반복에는 붙이지 않는다.
  - 본문·그림 하단 설명·figcaption 등 **문장 안에서는 괄호**로: `존(zone)`, `애니캐스트(anycast)`, `레지스트라(registrar)`.
  - **그림 라벨은 tspan**으로 한글 뒤에 작은 영문을 붙인다: `리졸버 Resolver`, `권한 네임서버 Authoritative Nameserver`. 마크업은 아래 「인라인 SVG 다이어그램」 참조.
  - 생략해도 되는 것: 이미 병기한 용어의 반복(그림마다 다시 붙이지 않는다), 일상어에 가까운 외래어(브라우저, 캐시), 이미 영문인 것(NXDOMAIN, TTL).

### 정확성

- **결론만 던지지 말고 메커니즘을 적는다.** 비유는 원인을 설명하지 못한다.
  - ✗ 둘이 서로 다른 층을 보고 있으니, 한 층에 옛 답이 남아 있으면 **답이 갈린다.**
  - ✓ `dig`와 `curl`은 서로 다른 층에 묻고, **층마다 캐시가 따로 있다. 한 층의 캐시에 만료되지 않은 옛 응답이 남아 있으면 그 층을 지나는 경로만 계속 그 옛 응답을 받는다.**

- **단순화가 예외를 덮어서는 안 된다.** 한 줄로 잘라 말하기 전에 반례가 없는지 본다. 아래 ✗는 리졸버가 캐시의 사본으로 답하는 경우와 충돌한다.
  - ✗ (그림 라벨) 리졸버는 **답을 모른다**
  - ✓ (그림 라벨) **원본을 갖고 있지 않다** / **캐시에 사본이 있으면 그걸로 답한다** / **없으면 대신 찾아 나선다**

## 마크다운 함정 (한국어)

- **볼드가 괄호·백틱 같은 문장부호로 끝나면서 바로 뒤에 조사가 붙으면 강조가 깨진다.** Goldmark(CommonMark)가 닫는 `**`를 강조 종료로 인식하지 못해 별표가 화면에 그대로 노출된다. 영문 병기는 볼드 밖으로 뺀다.
  - ✗ `**글루 레코드(glue record)**다.` → 화면에 `**글루 레코드(glue record)**다.`
  - ✓ `**글루 레코드**(glue record)다.` → 정상
  - 한글로 끝나는 볼드 뒤의 조사는 문제없다: `**NS 레코드**다.` → 정상
  - 저장소 전체 점검: ``grep -nE '[)`")」』.,]\*\*[가-힣]' content/posts/*.md`` (0건이어야 정상)
  - 빌드 후 확인: 렌더된 HTML 본문에 리터럴 `**`가 남아 있으면 깨진 것이다.

## 인라인 SVG 다이어그램

`<figure class="dgm">` 안의 좌표는 전부 손으로 배치한 값이다. 라벨 글자 수가 바뀌면 정렬이 깨질 수 있다.

- **영문 병기 tspan 패턴** — 한글 글자 크기의 0.7배, 굵기 400, 흐린 색:
  ```html
  <text x="40" y="52" font-size="18" font-weight="700" fill="currentColor">리졸버 <tspan dx="8" font-size="12.5" font-weight="400" fill="var(--secondary)">Resolver</tspan></text>
  ```
  진한 배경 카드 위에서는 `fill="rgba(255,255,255,.7)"`.
- **SVG는 `<tspan>` 앞뒤의 공백을 지운다.** 마크업에 스페이스를 넣어도 `리졸버Resolver`로 붙어 나온다. 띄어쓰기는 반드시 `dx="6~8"`로 준다.
- **꼬리표는 영문 앞에 둔다.** `스텁 리졸버 <tspan>Stub Resolver</tspan> · OS`로 쓰면 `· OS`가 큰 글씨로 영문에 달라붙어 읽히지 않는다. `스텁 리졸버 · OS<tspan>Stub Resolver</tspan>` 순서로 쓴다.
- **라벨을 고쳤으면 넘침을 확인한다.** `hugo server`로 띄우고 브라우저에서 각 `<text>`의 `getBBox()` 오른쪽 끝을 그 텍스트를 감싸는 `<rect>`의 오른쪽 변, 그리고 viewBox 폭과 비교한다. 어림값은 한글 = 글자당 폰트 크기, 영문 = 그 절반.
  ```js
  // .dgm svg 전체를 훑어 카드/viewBox를 넘는 라벨만 출력
  document.querySelectorAll('.dgm svg').forEach((svg,i)=>{
    const vb=svg.viewBox.baseVal;
    const rects=[...svg.querySelectorAll('rect')].map(r=>({x:r.x.baseVal.value,y:r.y.baseVal.value,w:r.width.baseVal.value,h:r.height.baseVal.value}));
    svg.querySelectorAll('text').forEach(t=>{
      const b=t.getBBox(), right=b.x+b.width, cy=b.y+b.height/2;
      const host=rects.filter(r=>b.x>=r.x&&b.x<=r.x+r.w&&cy>=r.y&&cy<=r.y+r.h).sort((a,c)=>a.w*a.h-c.w*c.h)[0];
      if(right>vb.width-2||(host&&right>host.x+host.w-6)) console.log(i+1,t.textContent,right);
    });
  });
  ```

## 수정 작업 시

- 한 파일을 고치면 짝이 되는 다른 언어 파일도 같이 본다. 내용·구조에 관한 수정은 양쪽에 미러링하고, 한국어 어법에만 해당하는 수정(조사·어순·번역투 등)은 국문만 고친다.
- 앞선 글을 인용·참조하는 서술은 실제로 그 글을 열어 확인한다. 예: 이 블로그의 DNS 글은 `aa` 플래그의 출처를 「도메인 사서 웹페이지 붙이기」로 적고 있었지만, 실제로 `aa`를 설명한 글은 「도메인 등록, 그림으로 이해하기」였다.
